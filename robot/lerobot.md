# Lerobot Docs

## Set up

Basically just follow [these](https://wiki.seeedstudio.com/lerobot_so100m_new/#install-lerobot) steps. 

After that you can use the `record.sh` script in the robot directory to record a subroutine, and run the
corresponding `replay*.sh` script to replay it. 
